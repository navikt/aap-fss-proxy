info:
  nais:
    cluster: ${nais.cluster.name}
    image : ${nais.app.image}
    app : ${nais.app.name}
    namespace: ${nais.namespace}
sts:
  base-uri: https://security-token-service.dev.adeo.no
joark:
  base-uri: https://dokarkiv-q2.nais.preprod.local
management :
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include:  '*'
  endpoint:
    health:
      show-details : always
spring:
  mvc:
    log-request-details : true